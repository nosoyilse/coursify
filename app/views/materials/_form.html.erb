<%= form_for [@course, @material] do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %><br/>
	<%= f.label :description %>
	<%= f.text_area :description %><br/>
	<%= f.label :file %>
	<%= f.text_field :file %><br/>
	<%= f.submit "Guardar" %>
<% end %>